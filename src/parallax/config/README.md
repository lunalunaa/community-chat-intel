# Parallax configuration files
#
# These YAML files provide the customizable parts of the pipeline that used
# to be hardcoded in Python source. Each file has sensible example defaults —
# edit them to match your own product/community ecosystem.
#
# All scripts look for config via the PARALLAX_CONFIG_DIR env var (default:
# the bundled defaults in src/parallax/config/). Point it at your own
# directory to override without touching the installed package.
#
#   export PARALLAX_CONFIG_DIR=~/my-parallax-config/
